
scheduledjobs {
    test-batch-actor {
        batch-size = 30
        fetch-size = 100
        batch-interval = 5 seconds
        retries = 3
        fetch-retry-delay = 3 seconds
        do-process = true
        schedule {
            schedule-interval = 5 minutes
            anchor-time {
            }
        }
    }
}